using System;

namespace DevCSharp
{

    class URI1001
    {
        static void Uri1001(string[] args)
        {
            int A, B, X;

            A = int.Parse(Console.ReadLine());
            B = int.Parse(Console.ReadLine());
            X = A + B;
            Console.WriteLine("X = " + X.ToString());
        }
    }
}
